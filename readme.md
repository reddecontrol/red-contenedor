echo "# red-contenedor" >> README.md 
git init 
git add README.md 
git commit -m "first commit" 
git branch -M master 
git remote add origin https://github.com/reddecontrol/red-contenedor. git
 git push -u origen maestro
